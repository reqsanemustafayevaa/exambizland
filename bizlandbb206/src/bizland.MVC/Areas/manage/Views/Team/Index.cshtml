@model List<TeamMember>
@{
    int counter = 0;
}
<div class="container">
    <div style="display:flex  justify-content-between align-items-center">
        <h1>Team:</h1>
        <a asp-action="create" asp-controller="team" class="btn btn-primary">Create</a>
    </div>
    <table class="table">
        <thead>
            <tr>
                <th scope="col">No</th>
                <th scope="col">Name</th>
                <th scope="col">Profession</th>
                <th scope="col">Image</th>

                <th scope="col">Action</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var tm in Model)

            {
                counter++;
                <tr>
                    <th scope="row">@counter</th>
                    <td>@tm.Name</td>
                    <td>@tm.Profession</td>
                    <td style="max-width:120px"><img src="~/uploads/teammembers/@tm.ImageUrl" /></td>
                    <td>
                        <a asp-action="update" asp-controller="team" class="btn btn-success">update</a>
                        <a asp-action="delete" asp-controller="team" class="btn btn-danger">delete</a>
                    </td>

                </tr>
            }


        </tbody>
    </table>
</div>


